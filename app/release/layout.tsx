import { Providers } from "../providers";

export default function ReleaseLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
                <div>
                    {children}
                </div>
    );
}
